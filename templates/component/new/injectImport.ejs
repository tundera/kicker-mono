---
inject: true
to: 'src/index.ts'
append: true
skip_if: export * from './<%= name %>'
---
<% component = h.camelizedBaseName(name) -%>
export * from '<%= location ? `../${location}` : '.' %>/<%= component %>'