---
inject: true
to: <%= h.prependModelsPath("db/schema.prisma") %>
append: true
skip_if: "model <%= h.camelizedBaseName(name) %>"
sh: "yarn --cwd <%= h.modelsPath %> prisma format"
---
<% model = h.camelizedBaseName(name) -%>
/// <%= model %> description
model <%= model %> {
  id            String   @id
  createdAt     DateTime @default(now())
  updatedAt     DateTime
}